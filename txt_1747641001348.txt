import sys

# Custom Vision or prediction service credentials
prediction_url = "YOUR_PREDICTION_URL"
prediction_key = "YOUR_PREDICTION_KEY"

# Set image number based on arguments
img_num = 1
if len(sys.argv) > 1 and sys.argv[0].isdigit() and int(sys.argv[0]) in [1, 2]:
    img_num = int(sys.argv[0])

# Image URL using formatted string
img = f"https://github.com/MicrosoftLearning/AI-900-AIFundamentals/raw/main/data/vision/road-safety/road-safety-{img_num}.jpg"

# Example English book review of "Atomic Habits"
review_text = """
Atomic Habits by James Clear is a powerful and practical guide for anyone looking to improve their habits and ultimately, their life. 
The book presents actionable strategies rooted in psychology and behavioral science. What I love most is how Clear emphasizes identity change 
as the foundation for habit transformation. Rather than chasing goals, the book teaches you to become the kind of person who naturally achieves them. 
This mindset shift is incredibly empowering.
"""

# For demonstration: just print the values
print(f"Prediction URL: {prediction_url}")
print(f"Prediction Key: {prediction_key}")
print(f"Image URL: {img}")
print("Review Text:")
print(review_text)

